<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <title>Fruits!</title>
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>
    <h1>Fruits show page</h1>
    <%= fruit.name %> is <%= fruit.color %>

    <% if(fruit.readyToEat) { %>
        It is ready to eat
    <% } else { %>
        It is not ready to eat
    <% } %>

    It is added by <%= fruit.User.name %>

    <h3>Seasons: </h3>

        <% for (let i = 0; i < fruit.Seasons.length; i++) {%>
            <li>
                <%= fruit.Seasons[i].name %>
            </li>
        <% } %>

    <br/>
    <br/>
    <form action="/fruits/<%=fruit.id %>?_method=DELETE" method="POST">
        <input type="submit" value="DELETE <%= fruit.name %>" />
    </form>
    <br/>
    <br/>
    <nav>
        <a href="/fruits">Return to fruits homepage</a>
    </nav>
</body>
</html>